"""
Bounded collaboration runtime.

Slice 1 wires collaboration metadata + deterministic repair strategist paths through
`plan_runtime.finalize_execute_step_verification` and `arbitration_policy`.
Future slices attach live multi-role runs (OpenClaw/Cursor) here.
"""

COLLABORATION_RUNTIME_VERSION = 1
